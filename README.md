## clone repo
```bash
git clone -b cloudflare --single-branch --no-checkout https://github.com/Syuhadak27/spreadsheet-read-bot-telegram.git .
```

Rrename <code>config-sampel.js</code>  ke <code>config.js</code>

